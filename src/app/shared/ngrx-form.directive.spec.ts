import { NgrxFormDirective } from './ngrx-form.directive';

describe('NgrxFormDirective', () => {
  it('should create an instance', () => {
    const directive = new NgrxFormDirective();
    expect(directive).toBeTruthy();
  });
});
